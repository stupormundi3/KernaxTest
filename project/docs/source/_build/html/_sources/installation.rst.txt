Installation
============

To install the project, you just need to clone the repository and install the dependencies

.. code-block:: bash

   git clone https://github.com/UNamurCSFaculty/2526_INFOB318_Kernax
   cd SVMtesting
   pip install -r requirements.txt